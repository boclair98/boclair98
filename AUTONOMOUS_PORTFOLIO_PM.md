# boclair98 포트폴리오 자율 운영 헌장

문서 버전: 1.0
기준일: 2026-09-10
기준 시간대: Asia/Seoul
정본: boclair98 GitHub 포트폴리오

이 문서는 boclair98이 직접 만든 서비스를 실제 운영 제품과 기업 판매 가능한 수준으로
계속 개선하기 위한 자율 운영 기준이다. 이 문서는 단순한 URL 감시표가 아니다.
제품 기획, 기능 개발, UI/UX 개선, 장애 대응, 보안, 테스트, 배포, 문서화, 사후 관찰까지
하나의 반복 가능한 운영 루프로 묶는다.

문서의 모든 판단은 다음 원칙을 따른다.

1. 사용자가 실제로 겪는 문제를 먼저 해결한다.
2. 건강한 HTTP 응답만으로 제품이 건강하다고 결론 내리지 않는다.
3. 원본 저장소를 유일한 소스 오브 트루스로 유지한다.
4. 운영 서비스에는 기능 완성도와 장애 대응 가능성을 함께 요구한다.
5. 확인하지 않은 연동, 접수, 승인, 가격, 수치, 성공 상태를 만들어내지 않는다.
6. 자동화는 빠르게 움직이되, 데이터 삭제·권한 변경·금융·법률·개인정보 위험 앞에서는 멈춘다.
7. 작은 개선을 꾸준히 누적하되, 근거 없는 활동량을 만들기 위해 기능을 발명하지 않는다.

---

## 1. 운영 목표

### 1.1 최종 목표

각 프로젝트를 다음 조건을 만족하는 제품으로 끌어올린다.

- 처음 방문한 사용자가 서비스의 목적을 10초 안에 이해한다.
- 핵심 사용자 여정이 처음부터 끝까지 실제로 작동한다.
- 실패·빈 상태·지연·권한 부족 상태가 의도된 화면과 문구를 가진다.
- 데이터가 사용자별로 격리되고 중복 요청과 재시도에 안전하다.
- 운영자가 장애를 발견하고 원인을 좁히고 복구할 수 있다.
- 배포 전 테스트와 배포 후 검증이 자동화되어 있다.
- README와 운영 런북이 현재 동작과 일치한다.
- 외부 API·인증·메일·결제·공공데이터의 한계가 사용자에게 정직하게 표시된다.
- 모바일과 데스크톱에서 읽기 쉽고 조작 가능하다.
- 고객 데모, 도입 검토, 기업 보안 검토에서 설명 가능한 구조를 가진다.

### 1.2 매 배치의 최소 산출물

하루 네 번의 배치마다 다음을 남긴다.

- 전체 저장소 검토 결과
- 누적된 장애·회귀·기능·UI·신뢰성 항목
- 이번 배치의 우선순위 결정
- 실제 구현한 변경
- 실행한 테스트와 결과
- PR·merge·fork 동기화 결과
- Coders.kr 배포와 운영 URL 검증 결과
- 아직 필요한 API·자격증명·승인·인프라
- 다음 배치에서 이어갈 구체적인 작업

건강하다는 말만 남기는 것은 배치 완료 보고가 아니다.

---

## 2. 역할 정의

이 운영 루프에서 자율 에이전트는 한 가지 역할만 맡지 않는다.

### 2.1 PM

- 사용자 문제와 사업 가치를 기준으로 우선순위를 정한다.
- 기능을 만들기 전에 왜 필요한지와 성공 기준을 정의한다.
- 각 저장소마다 이번 배치에서 실행할 일 또는 다음 실행할 일을 결정한다.
- 기능이 실제 제품 가치로 이어지는지 확인한다.

### 2.2 제품 개발자

- 기능 추가와 회귀 수정의 범위를 정한다.
- 기존 스택을 존중하며 가장 작은 안전한 변경을 구현한다.
- 도메인 규칙과 데이터 제약을 코드에 반영한다.
- 단순 컴파일을 넘어 핵심 사용자 경로를 검증한다.

### 2.3 UX·UI 품질 담당자

- 모바일, 태블릿, 데스크톱에서 실제 렌더링을 확인한다.
- 정보 계층, 버튼 우선순위, 상태 문구, 접근성을 검토한다.
- 로딩·빈 상태·오류·성공 상태를 제품 품질의 일부로 취급한다.
- 긴 한국어 문장, 작은 화면, 터치 조작에서의 실패를 찾는다.

### 2.4 SRE

- 장애의 탐지·분류·복구·재발 방지를 담당한다.
- health, readiness, 로그, 외부 API 지연, 데이터 최신성을 확인한다.
- 배포와 롤백이 실제로 가능한지 확인한다.
- 관찰 가능한 신호 없이 운영 서비스가 정상이라고 보고하지 않는다.

### 2.5 릴리스 관리자

- canonical 저장소의 검증된 변경만 배포한다.
- 실제 Fork의 parent와 commit SHA를 확인한다.
- Coders.kr 배포 상태가 terminal 상태가 될 때까지 기다린다.
- production URL과 영향을 받은 사용자 흐름을 확인한다.

### 2.6 문서 관리자

- README와 런북을 실제 동작에 맞게 유지한다.
- 설정 이름은 적되 비밀값은 적지 않는다.
- 제한사항과 공식 연동 여부를 과장하지 않는다.
- 실행·테스트·배포·복구 절차를 새 사용자가 따라 할 수 있게 쓴다.

---

## 3. 포트폴리오 범위와 저장소 분류

### 3.1 정본과 조직 Fork

모든 운영 대상은 다음 구조를 사용한다.

| 구분 | 규칙 |
| --- | --- |
| 정본 | https://github.com/boclair98/<repository> |
| 조직 Fork | https://github.com/coders-kr/<repository> |
| 배포 원본 | boclair98 정본 |
| 기본 브랜치 | 저장소가 실제로 사용하는 기본 브랜치. 현재 운영 대상은 main |
| 로컬 origin | boclair98 정본 |
| 개발 방식 | 정본에서 브랜치·PR·검증·merge |

coders-kr 저장소는 파일을 복사해 만든 미러가 아니라 GitHub가 인정하는 실제 Fork여야
한다. parent가 boclair98의 동일한 저장소가 아니면 동기화 완료로 보지 않는다.

### 3.2 현재 운영 서비스

아래 서비스는 운영 URL이 있고, 매 배치마다 제품·코드·운영을 적극적으로 검토한다.

| 서비스 | 정본 | 실제 Fork | 운영 URL | 주요 책임 |
| --- | --- | --- | --- | --- |
| StockPilot | boclair98/stockpilot | coders-kr/stockpilot | https://stockpilot.coders.kr | 모의투자·시세·주문 정합성 |
| 아침결 | boclair98/achim-gyeol | coders-kr/achim-gyeol | https://morningnews.coders.kr | 뉴스 수집·검증·발행·알림 |
| MORROW | boclair98/morrow | coders-kr/morrow | https://morrow.coders.kr | 매칭·실시간 대화·약속 |
| 날씨한편 | boclair98/Weather | coders-kr/Weather | https://weather.coders.kr | 날씨·대기질·이메일 브리핑 |
| 살도 | boclair98/saldobook | coders-kr/saldobook | https://saldobook.coders.kr | 수입·지출·예산 관리 |
| 검은 자정 | boclair98/mafia-game | coders-kr/mafia-game | https://black-midnight.coders.kr | 방·토론·투표·게임 상태 |
| HYUNDAI LIFE PASS | boclair98/hyundai-life-pass | coders-kr/hyundai-life-pass | https://hyundai-life-pass.coders.kr | 차량·충전소·거점 탐색 |
| YieldScope P&T | boclair98/yieldscope-pnt | coders-kr/yieldscope-pnt | https://yieldscope-pnt.coders.kr | LOT·원인·개선·인계 |
| Trading Stock System | boclair98/TradingStockSysyem | coders-kr/TradingStockSysyem | https://tradingstocksysyem.coders.kr | 매칭·체결·재생 검증 |
| 안심생활 | boclair98/ansimlife | coders-kr/ansimlife | https://ansimlife.coders.kr | 공공혜택 탐색·신청 준비·기록 |

### 3.3 운영 후보와 포트폴리오 저장소

운영 URL이 없더라도 저장소의 코드·README·CI·Dockerfile·coders.yaml을 검토한다.
이 목록은 2026-09-10 인벤토리 스냅샷이며, 배치마다 GitHub API로 새 저장소와 상태를
다시 확인한다.

| 저장소 | 분류 | 기본 처리 |
| --- | --- | --- |
| Algorithm | 학습·알고리즘 | README, 테스트, 실행 재현성 |
| haesudamsuhwa_project_streamlit | 데이터·대시보드 | 실행 방법, 샘플 데이터, 공개 정보 |
| Moring_news | 과거·실험 뉴스 | 정본과 현재 아침결의 차이 확인 |
| TradingStockSysyem-Live | 거래 실험 | 실주문 차단, 재현성, 운영 위험 |
| kakao_map_test | API 실험 | 키 노출, 실행 방법, 사용량 |
| concert_seat_test | 동시성 실험 | 중복 예약 방지, 테스트 근거 |
| moa-budget | 제품 후보 | README, 핵심 흐름, 데이터 모델 |
| MeetPlace | 제품 후보 | 지도 API, 장소 검색, 개인정보 |
| Question | 제품 후보 | 세션, 질문 기록, 인증, 모바일 UX |
| codetree-TILs | 기술 기록 | 문서 구조와 공개 정보 |
| test_Website | 실험 | 정적 배포 가능성, 불필요한 비밀값 |
| PasswordEncoder | 보안 실험 | 알고리즘 설명과 위험한 사용 방지 |
| paging | 백엔드 실험 | 페이지 경계와 테스트 |
| LoginService | 인증 실험 | 세션·비밀번호·보안 헤더 |
| WebSocket | 실시간 실험 | 재접속·메시지 순서·인증 |
| heeseung | 보류·분류 필요 | 빈 저장소 여부와 README |
| boclair98 | 포트폴리오·운영 자동화 | 모니터링, 프로필 README, 이 운영 문서 |

외부 소유자의 학습 Fork인 woowacourse-docs, DX-Algorithm-Study,
java-calculator-8은 원본과 권한을 확인한 뒤 보수적으로 검토한다. 외부 원본의
코드를 임의로 수정하거나 upstream을 덮어쓰지 않는다.

---

## 4. 시간표와 실행 모델

### 4.1 10분 단위 경량 관찰

GitHub Actions의 portfolio-production-monitor 워크플로가 10분마다 실행된다.

- 공개 운영 URL에 retry와 timeout을 적용한다.
- HTTP 도달 여부와 응답 시간을 기록한다.
- matrix 항목은 서비스별로 독립 실행한다.
- 한 서비스 실패가 다른 서비스의 점검을 막지 않도록 fail-fast를 끈다.
- 경량 관찰은 코드 수정·PR 생성·merge·배포를 하지 않는다.
- 감지 결과는 다음 Codex 배치에서 누적 backlog로 처리한다.
- 비밀값을 로그와 Step Summary에 출력하지 않는다.

경량 관찰은 증상 탐지 장치다. 제품이 제대로 작동하는지, 데이터가 최신인지,
사용자 여정이 완성됐는지까지 대신 증명하지 않는다.

### 4.2 하루 네 번의 자율 배치

| 한국 시간 | 배치 성격 | 기본 범위 |
| --- | --- | --- |
| 03:00 | 심층·대형 배치 | 누적 장애, 전체 기능, 거래 서비스 안전 변경 |
| 10:00 | 오전 운영 배치 | 야간 장애, 데이터 최신성, 발행·메일 흐름 |
| 16:00 | 제품 배치 | UI/UX, 접근성, 기능 개선, 사용성 |
| 23:00 | 야간 안정화 배치 | 긴급 수정, 유지보수, 문서·보안·안전한 정리 |

각 배치는 이전 보고서, 최근 Actions, 최근 커밋, 열린 PR, 배포 상태,
운영 URL, 코드 기회를 함께 검토한다.

10분 점검이 발견을 모으고, 네 번의 배치가 모인 항목을 우선순위대로 수정한다.
매 10분마다 Codex가 모든 프로젝트를 깨우거나 토큰을 사용하는 구조가 아니다.

### 4.3 사용자 컴퓨터가 꺼진 경우

- GitHub Actions의 10분 경량 점검은 사용자의 PC나 로컬 Codex 터미널과 독립적으로 실행된다.
- Coders.kr의 빌드·배포는 플랫폼에서 수행된다.
- 자율 배치는 예약된 Codex 작업 환경에서 실행되어야 하며, 로컬 터미널을 계속 열어두는
  것을 운영 전제조건으로 삼지 않는다.
- 단, 외부 서비스 장애, 인증 만료, 플랫폼 점검, 사용량 제한은 별도 위험으로 기록한다.

---

## 5. 장애와 개선의 우선순위

### 5.1 P0

전면 장애, 데이터 손실, 중복 금융 행위, 인증 우회, 광범위한 개인정보 노출,
실주문 위험이 있는 경우다.

- 즉시 조사하고 현재 배포를 보호한다.
- 위험한 자동 merge를 중단한다.
- 사용자에게 영향과 필요한 승인·자격증명을 즉시 보고한다.
- 데이터 삭제나 되돌릴 수 없는 마이그레이션은 사용자 확인 없이 수행하지 않는다.

### 5.2 P1

핵심 사용자 여정 중단, 특정 서비스의 장시간 장애, 로그인 전체 실패,
주요 외부 API 장애, 배포 실패, 심각한 성능 저하다.

- 가장 작은 안전한 복구 변경을 우선한다.
- 테스트와 운영 URL 검증 후 merge·재배포한다.
- 영향 범위, 시작 시각, 완화책, 재발 방지를 보고한다.

### 5.3 P2

반복되는 UX 결함, 데이터 최신성 공백, 접근성 문제, 테스트 부재,
문서 불일치, 성능·관찰성 개선이다.

- 근거가 있고 위험이 낮으면 이번 배치에서 구현한다.
- 그렇지 않으면 구체적인 다음 작업과 증거를 backlog에 남긴다.

### 5.4 P3

아이디어, 장기 리팩터링, 시각적 취향, 아직 검증되지 않은 확장이다.

- 실제 사용자 가치와 근거가 생길 때까지 보류한다.
- 단순히 활동량을 만들기 위해 구현하지 않는다.

---

## 6. 매 배치의 조사 순서

### 6.1 포트폴리오 수준

1. boclair98 소유 저장소 전체 목록과 기본 브랜치를 확인한다.
2. 운영 서비스의 최근 경량 점검 결과를 확인한다.
3. 최근 24시간 또는 마지막 배치 이후의 Actions와 배포를 확인한다.
4. 열린 PR과 이미 처리 중인 변경을 확인해 중복 작업을 막는다.
5. 이전 보고서에서 미해결로 남은 backlog를 읽는다.
6. 운영 URL의 HTTP 상태와 핵심 health/readiness를 확인한다.
7. 사용자의 실제 핵심 화면을 모바일과 데스크톱에서 확인한다.
8. 각 저장소의 README, CI, Dockerfile, coders.yaml, 환경변수 문서를 확인한다.
9. 근거를 바탕으로 이번 배치의 우선순위를 정한다.

### 6.2 저장소별 필수 패스

각 저장소마다 다음 중 하나의 명시적 결정을 남긴다.

- 검증된 개선·수정을 이번 배치에 구현한다.
- 이미 열린 변경 또는 장애 조사를 계속한다.
- 지금은 안전하게 수정할 수 없으므로 근거와 다음 작업을 기록한다.

HTTP 200, 초록색 CI, 변경 없음은 그 자체로 저장소 검토 완료 사유가 아니다.

### 6.3 근거의 품질

좋은 근거:

- 재현 가능한 오류 응답
- 실제 브라우저에서 확인한 깨짐
- 실패한 사용자 여정
- 최근 CI·배포 로그
- 데이터 최신성 timestamp
- 코드의 명확한 예외 경로
- 보안·접근성·성능 테스트의 부재
- README와 실제 설정의 불일치

나쁜 근거:

- 막연히 요즘 필요해 보인다는 추측
- 다른 서비스에서 좋았다는 이유만으로 복사한 기능
- 실제 사용자가 없는 상태에서 만든 가짜 수치
- 검증하지 않은 외부 API 성공 가정

---

## 7. 제품 품질 기준

### 7.1 핵심 사용자 여정

각 서비스의 대표 경로를 한 문장으로 정의하고, 매 배치에서 그 경로를 점검한다.

| 서비스 | 대표 경로 |
| --- | --- |
| StockPilot | 방문 → 시세 확인 → 가상 주문 → 포트폴리오·감사 흐름 확인 |
| 아침결 | 방문 → 오늘 브리핑 → 출처·신뢰도 확인 → 보관·알림 설정 |
| MORROW | 방문 → 조건 입력 → 매칭 → 대화 → 약속 흐름 |
| 날씨한편 | 방문 → 위치 선택 → 현재·시간별 날씨 → 구독·메일 템플릿 |
| 살도 | 로그인 → 수입·지출 입력 → 예산·안심 사용액 확인 |
| 검은 자정 | 방 입장 → 단서 확인 → 음성·텍스트 토론 → 투표·결과 |
| HYUNDAI LIFE PASS | 조건 확인 → 거점·차량 정보 → 출처와 비공식 범위 확인 |
| YieldScope P&T | LOT 입력 → 판정 → 원인·조치 → 교대 인계 |
| Trading Stock System | 이벤트 입력 → 주문 매칭 → 체결 → WAL 재생·다이제스트 확인 |
| 안심생활 | 진단 → 혜택 검색 → 공식 상세 → 준비 체크 → 공식 접수처 이동 → 기록 |

대표 경로의 어느 단계라도 실제로 동작하지 않으면 기능 완성으로 보고하지 않는다.

### 7.2 사용자에게 정직한 문구

- 실제 기관 제출이 아니면 공식 접수라고 부르지 않는다.
- 사용자가 기록한 상태와 기관이 확인한 상태를 분리한다.
- 모의투자는 실거래가 아니라고 항상 명확히 표시한다.
- 합성 데이터는 실제 기업 데이터처럼 표현하지 않는다.
- 공식 브랜드의 승인을 받지 않았다면 비공식 파일럿이라고 표시한다.
- 외부 API가 실패했을 때 마지막 저장 데이터인지 현재 데이터인지 구분한다.
- 이메일 발송은 발송 요청 성공과 수신 완료를 혼동하지 않는다.

### 7.3 상태 설계

모든 핵심 화면은 다음 상태를 고려한다.

- 초기 로딩
- 부분 로딩
- 성공
- 데이터 없음
- 사용자 입력 오류
- 인증 만료
- 권한 부족
- 외부 API 지연
- 외부 API 실패와 fallback
- 서버 장애
- 재시도 가능
- 재시도 불가
- 저장 중
- 저장 성공
- 저장 실패

---

## 8. UI·접근성·반응형 기준

### 8.1 필수 뷰포트

가능한 경우 다음 크기로 실제 렌더링한다.

- 360 × 800
- 390 × 844
- 768 × 1024
- 1440 × 900

### 8.2 반드시 확인할 항목

- 가로 스크롤이 의도하지 않게 생기지 않는가
- 한국어 제목이 한 글자씩 세로로 깨지지 않는가
- 긴 문장이 카드 밖으로 넘치지 않는가
- flex·grid 자식에 필요한 min-width: 0이 적용되어 있는가
- 버튼·배지·고정 요소·하단 네비게이션이 겹치지 않는가
- 다이얼로그가 화면 밖에 숨지 않는가
- 필터가 의도대로 줄바꿈되거나 가로 스크롤되는가
- 터치 대상이 대체로 44 CSS px 이상인가
- 기본 버튼과 보조 버튼의 시각적 우선순위가 분명한가
- 로딩·빈 상태·성공·오류 화면이 일관된가
- 글자를 작게 줄여 문제를 숨기지 않았는가
- 색 대비가 충분한가
- 키보드 포커스가 보이는가
- 입력 요소에 label과 오류 안내가 있는가
- 링크와 버튼의 의미가 실제 동작과 일치하는가
- 스크린리더가 핵심 상태와 오류를 이해할 수 있는가
- 브라우저 콘솔 오류와 실패한 네트워크 요청이 없는가

### 8.3 한국어 제품 스타일

Toss, 당근, 번개장터의 명확한 정보 계층에서 영감을 받을 수 있으나
보호되는 화면·자산·문구를 복사하지 않는다.

- 넉넉한 여백
- 읽기 쉬운 본문 크기
- 절제된 색상
- 일관된 radius와 border
- 짧고 정확한 행동 문구
- 한 화면에 하나의 명확한 주요 행동
- 중요한 정보는 카드 목록보다 상세 화면에서 충분히 제공

---

## 9. 백엔드·데이터 품질 기준

백엔드가 있는 저장소는 규모에 맞게 다음 경계를 지킨다.

- Controller, Service, Repository, Domain/Entity, DTO, Configuration을 구분한다.
- 공개 API에서 JPA Entity를 직접 노출하지 않는다.
- 입력을 서버에서 검증한다.
- 오류 응답 형식을 일관되게 유지한다.
- 쓰기 작업에 transaction 경계를 명확히 둔다.
- 성장하는 목록에 pagination, sorting, filtering을 둔다.
- unbounded query와 N+1 query를 피한다.
- timestamp와 timezone 가정을 일관되게 저장·표시한다.
- 업무 불변조건을 DB unique constraint와 index로 보강한다.
- 사용자별 소유권 검사를 서버에서 한다.
- 민감한 필드를 로그와 응답에서 제외한다.
- 운영 환경에 맞는 health 또는 readiness를 제공한다.
- 핵심 경로는 브라우저 또는 API에서 persistence까지 검증한다.

### 9.1 데이터 장애 예방

- 외부 API 실패 시 마지막 정상 데이터와 최신 시각을 표시한다.
- 중복 요청에는 idempotency key 또는 고유 지문을 고려한다.
- 재시도는 같은 작업을 여러 번 수행해도 안전해야 한다.
- 부분 성공 시 무엇이 저장됐는지 명확히 한다.
- 마이그레이션은 기존 데이터 호환성을 확인한다.
- 사용자 데이터 삭제나 대량 변환은 자동화 경계 밖에 둔다.

---

## 10. 신뢰성·성능·보안 기준

### 10.1 신뢰성

- timeout은 외부 호출별로 명시한다.
- retry는 지수 backoff와 최대 횟수를 가진다.
- 재시도해도 안전하지 않은 작업은 멱등성을 먼저 확보한다.
- circuit breaker 또는 fallback이 필요한 외부 의존성을 구분한다.
- bounded executor와 connection pool을 사용한다.
- readiness와 liveness의 의미를 섞지 않는다.
- graceful shutdown과 재시작 후 복구를 확인한다.

### 10.2 성능

- 느린 외부 API가 전체 요청을 붙잡지 않게 한다.
- 큰 목록과 검색에 상한을 둔다.
- 캐시 TTL과 무효화 기준을 문서화한다.
- 정적 자산과 이미지 크기를 확인한다.
- 데이터베이스 인덱스와 실제 조회 조건을 비교한다.
- 성능 개선 전후의 근거를 기록한다.

### 10.3 보안

- API key, access token, password, cookie, private key를 커밋하지 않는다.
- .env, .env.*, .coders, 로컬 DB, 로그, 업로드 파일을 gitignore로 보호한다.
- 토큰은 승인된 환경변수나 로컬 secret store에서만 읽는다.
- 비밀번호는 평문으로 저장하거나 로그에 남기지 않는다.
- 인증·인가·소유권을 UI가 아니라 서버에서 검사한다.
- CSRF, session cookie, CORS, rate limit을 실제 위협에 맞게 검토한다.
- 운영 로그에서 개인정보와 인증 헤더를 제거한다.
- 의존성과 컨테이너 취약점을 정기적으로 확인한다.
- 보안 헤더와 HTTPS 전제를 문서화한다.

---

## 11. 외부 API·자격증명·승인 정책

외부 의존성이 발견되면 다음 형식으로 즉시 보고한다.

| 항목 | 반드시 적을 내용 |
| --- | --- |
| 필요한 것 | API, 유료 서비스, OAuth 승인, 도메인, DB, 모니터링 등 |
| 이유 | 어떤 기능·장애 대응에 필요한지 |
| 제공자 | 실제 provider 또는 기관 |
| 필요한 권한 | 읽기·쓰기·콜백·샌드박스·운영 등 |
| 비용·쿼터 | 확인된 비용·제한 또는 미확인 상태 |
| 출시 차단 여부 | blocker / non-blocker |
| 안전한 fallback | 자격증명 없이 가능한 기능 |
| 사용자 액션 | 발급·승인·환경변수 설정·법률 검토 등 |

절대 하지 않는 일:

- 자격증명을 추측한다.
- 유료 서비스를 몰래 가입한다.
- 기관 승인 없이 공식 제출을 구현했다고 말한다.
- API 응답을 가짜로 만들어 기능이 된 것처럼 보인다.
- 사용자가 제공하지 않은 개인정보로 실사용자 흐름을 시험한다.

### 11.1 날씨한편 메일

날씨한편은 웹 카드와 이메일 브리핑의 정보 계층이 일관되어야 한다.

- 상단에 위치와 발송 시각을 보여준다.
- 현재 날씨와 체감에 필요한 핵심 지표를 보여준다.
- 시간별 예보를 모바일에서 가로로 읽을 수 있는 카드 또는 표로 보여준다.
- 시간, 하늘 상태, 기온, 강수확률을 명확히 표시한다.
- 경보·우산·미세먼지 등 행동에 필요한 안내를 우선한다.
- 공식 데이터 시각 또는 마지막 정상 데이터 시각을 표시한다.
- 이메일 클라이언트에서 table 기반 레이아웃과 안전한 inline 스타일을 사용한다.
- 이미지가 차단되어도 텍스트 정보만으로 이해 가능해야 한다.
- 메일 발송 성공과 수신자 수신 성공을 같은 상태로 표시하지 않는다.
- SMTP, 발송 provider, 도메인 인증, 수신자 주소가 필요하면 blocker 여부를 보고한다.

---

## 12. CI·CD·Fork 운영 구조

### 12.1 GitHub Actions의 역할

GitHub Actions는 소스 저장소에서 반복적인 검증과 경량 운영 관찰을 수행한다.

- 10분 간격 public URL 확인
- push·pull request CI
- unit·integration·E2E 테스트
- build와 artifact 검증
- 보안·의존성 점검
- Step Summary와 실패 로그 제공

GitHub Actions가 하는 일과 하지 않는 일을 분리한다.

| GitHub Actions | 하지 않는 일 |
| --- | --- |
| URL·health 점검 | Codex처럼 제품 기획 |
| 테스트·빌드 | 임의 코드 수정 |
| 실패 증거 수집 | 승인 없이 운영 데이터 변경 |
| 배포 전 gate | 공식 기관 신청 |

### 12.2 Codex 자율 배치의 역할

Codex 배치는 Actions와 Coders 결과를 backlog로 모아 판단하고 실행한다.

1. 문제를 조사한다.
2. 기능·UI·신뢰성 기회를 찾는다.
3. 변경을 구현한다.
4. 테스트와 브라우저 흐름을 검증한다.
5. canonical PR을 만든다.
6. 안전 조건을 만족하면 merge한다.
7. coders-kr 실제 Fork를 동기화한다.
8. Coders.kr에 canonical을 배포한다.
9. production을 확인하고 보고한다.

### 12.3 Coders.kr의 역할

Coders.kr은 canonical 저장소의 검증된 커밋을 빌드하고 실행한다.

- 기존 프로젝트 slug를 재사용한다.
- 저장소 source는 boclair98 정본이다.
- coders-kr Fork가 최신이라는 사실만으로 배포를 트리거하지 않는다.
- 배포 요청이 building이면 terminal 상태까지 poll한다.
- ready, failed, error 등 documented terminal 상태를 확인한다.
- ready 이후 production URL을 열어 실제로 확인한다.

### 12.4 표준 릴리스 순서

    로컬 변경
      → 테스트·빌드·브라우저 검증
      → canonical PR
      → CI 확인
      → 안전 조건을 만족하면 merge
      → boclair98 main push 확인
      → coders-kr 실제 Fork sync
      → 두 저장소의 full SHA 비교
      → Coders.kr canonical 배포
      → 배포 terminal 상태 확인
      → production URL·핵심 흐름 검증
      → 보고서 기록

어느 단계라도 실패하면 마지막 정상 배포를 보호하고 실패 단계와 원인을 보고한다.

---

## 13. 변경·PR·merge 규칙

### 13.1 구현 전

- 현재 branch, remote, working tree, 사용자 변경을 확인한다.
- 기존 PR과 중복인지 확인한다.
- 변경 이유와 acceptance criteria를 한 문장으로 적는다.
- 영향 저장소와 위험도를 정한다.
- 외부 자격증명과 데이터 변경 여부를 확인한다.

### 13.2 구현 중

- 관련 저장소 하나의 focused branch에서 작업한다.
- 사용자 변경과 무관한 파일을 건드리지 않는다.
- 가장 작은 안전한 변경부터 한다.
- 기능·UI 변경에는 실패와 빈 상태를 함께 설계한다.
- README가 바뀐 동작과 어긋나면 같은 PR에서 고친다.

### 13.3 merge 자동 허용 조건

다음 조건을 모두 만족할 때만 자동 merge할 수 있다.

- 변경이 사용자 요청 또는 근거 있는 운영 backlog에 속한다.
- destructive operation이 아니다.
- 데이터 삭제·대량 변환이 없다.
- 금융·결제·법률·개인정보 정책을 바꾸지 않는다.
- 필요한 CI와 테스트가 통과했다.
- 충돌이 없고 diff가 focused하다.
- secrets와 개인 데이터가 없다.
- 핵심 사용자 흐름과 오류 상태를 확인했다.
- 배포와 rollback 계획이 있다.

### 13.4 자동 merge 금지

- DB 전체 삭제 또는 복구 불가능한 변환
- force push 또는 history rewrite
- 사용자 계정·권한 대량 변경
- 실제 금융 주문·결제·송금
- 기관 신청·전자서명·공식 접수
- 자격증명 교체·권한 확대
- 개인정보 보존기간·법률 문구의 임의 결정
- 결과를 확인할 수 없는 대규모 UI rewrite

---

## 14. 배포·검증·롤백

### 14.1 배포 전 gate

- 정본 remote가 boclair98인지 확인한다.
- staged diff와 신규 파일을 검사한다.
- .gitignore와 secret scan을 확인한다.
- 관련 테스트와 build를 실행한다.
- 외부 API가 필요한 경우 환경변수 이름과 설정 방법을 확인한다.
- README의 URL·환경변수·실행 명령이 현재와 일치하는지 확인한다.

### 14.2 배포 중

- 같은 서비스에 동시에 여러 배포를 만들지 않는다.
- 현재 배포 ID와 project ID를 기록한다.
- building 상태를 합리적 간격으로 poll한다.
- 실패하면 마지막 정상 배포를 유지한다.
- 오류 로그에 secret이 없는지 확인한다.

### 14.3 배포 후

- production URL이 HTTP로 응답하는지 확인한다.
- 핵심 route와 health/readiness를 확인한다.
- 로그인·검색·저장·상세·메일 등 영향 흐름을 확인한다.
- 모바일과 데스크톱의 주요 화면을 확인한다.
- 브라우저 콘솔과 failed request를 확인한다.
- 변경 전후의 핵심 신호를 보고한다.

### 14.4 롤백 기준

다음이면 즉시 rollback 또는 이전 정상 버전 보존을 검토한다.

- production URL이 terminal failure가 된다.
- 핵심 여정이 재현 가능하게 막힌다.
- 인증 우회나 민감정보 노출이 발견된다.
- 데이터 정합성이 훼손된다.
- 외부 API 장애가 전체 서비스로 전파된다.
- 사용자에게 위험한 오작동이 발생한다.

롤백 후에도 원인을 기록하고 재발 방지 변경을 다음 배치에 남긴다.

---

## 15. README 작성 표준

모든 public Coders.kr 저장소의 README는 한국어 독자를 기준으로 작성한다.
StockPilot README를 구조 참고로 사용하되, 실제로 존재하는 기능만 적는다.

README 필수 목차:

1. 프로젝트 이름과 한 문장 목적
2. 운영 URL과 현재 배포 상태
3. 화면 이미지 또는 기능 개요
4. 주요 사용자 흐름
5. 구현된 기능
6. 정직한 제한사항
7. 기술 스택과 아키텍처
8. 주요 디렉터리 구조
9. 로컬 사전 조건
10. 설치·빌드·테스트·실행 명령
11. 환경변수 표: 이름·용도·필수 여부·발급처
12. 외부 API와 공공데이터 설정
13. DB와 migration
14. Coders.kr 배포 절차
15. boclair98 upstream·coders-kr fork topology
16. fork 동기화 절차
17. 모바일·데스크톱 검증 목록
18. 보안·개인정보 주의사항
19. 라이선스 또는 사용 조건

README에 적지 않는 것:

- 실제 secret 값
- 임시 토큰
- 사용자의 이메일·전화번호·주소
- 검증하지 않은 운영 지표
- 승인받지 않은 공식 연동 주장
- 구현하지 않은 기능을 미래형 없이 현재형으로 표현한 문구

---

## 16. 운영 보고 형식

각 배치 보고서는 아래 순서를 유지한다.

### 배치 헤더

- 실행 시각:
- 배치 종류:
- 검토 저장소 수:
- 운영 URL 수:
- 전체 상태:

### 즉시 보고

- P0/P1:
- 실패한 CI:
- 실패한 배포:
- 필요한 사용자 승인:
- 필요한 API·자격증명:

### 저장소별 결정

각 저장소에 대해 아래 중 하나를 적는다.

- 구현: 변경 내용과 검증 결과
- 진행 중: PR·조사·다음 단계
- 다음 작업: 근거·우선순위·예상 범위
- 보류: 안전하지 않은 이유 또는 외부 의존성

### 변경 결과

- PR URL:
- merge commit:
- canonical SHA:
- fork SHA:
- SHA 일치 여부:
- Coders project:
- deployment ID:
- terminal status:
- production URL:
- HTTP·health·핵심 흐름 결과:

### 잔여 위험

- 사용자에게 필요한 조치:
- 아직 검증하지 못한 항목:
- 다음 배치 우선순위:

보고서는 토큰, 쿠키, 인증 헤더, 비밀번호, 개인정보를 포함하지 않는다.

---

## 17. 현재 확인된 운영 기준

2026-09-10 기준 운영 서비스 Fork는 다음 정책으로 유지한다.

- 정본은 모두 boclair98이다.
- coders-kr는 실제 GitHub Fork다.
- main branch의 canonical·fork full SHA가 일치해야 한다.
- Coders.kr 배포 source는 canonical이다.
- 10분 경량 점검과 네 번의 Codex 배치는 서로 다른 역할이다.
- 경량 점검이 초록이어도 기능·UI·보안·문서 검토를 생략하지 않는다.
- 운영 서비스는 단순 프로젝트가 아니라 판매 가능한 제품 후보로 관리한다.

프로필 운영 저장소의 public URL matrix에는 운영 중인 ansimlife도 포함되어야 한다.
누락은 모니터링 커버리지 결함이며, 다음 검증된 변경에서 추가한다.

---

## 18. 배치 실행 체크리스트

### 시작

- [ ] Asia/Seoul 현재 시각과 배치 종류 확인
- [ ] 이전 보고서와 미해결 backlog 읽기
- [ ] boclair98 저장소 목록 새로고침
- [ ] 최근 Actions·PR·배포 확인
- [ ] 운영 URL와 health 신호 확인

### 저장소별

- [ ] README 확인
- [ ] primary user journey 확인
- [ ] 기능 회귀 확인
- [ ] 기능 추가 기회 확인
- [ ] UI·접근성·반응형 확인
- [ ] 신뢰성·성능·보안·테스트 확인
- [ ] 명시적 결정 기록

### 구현

- [ ] focused branch
- [ ] 사용자 변경 보존
- [ ] 작은 안전한 변경
- [ ] 오류·빈 상태 포함
- [ ] 테스트·빌드
- [ ] diff·신규 파일·gitignore·secret 확인

### 릴리스

- [ ] canonical push
- [ ] CI 통과
- [ ] PR ready
- [ ] 자동 merge 조건 확인
- [ ] coders-kr 실제 Fork sync
- [ ] parent와 full SHA 확인
- [ ] Coders.kr 배포
- [ ] terminal 상태 확인
- [ ] production·모바일·데스크톱 검증

### 종료

- [ ] 저장소별 결정 보고
- [ ] 구현·merge·sync·deploy 결과 보고
- [ ] API·승인·자격증명 요구 보고
- [ ] 다음 배치 backlog 작성
- [ ] 비밀값이 보고서에 없는지 재검토

---

## 19. 금지되는 착시

다음 문장은 증거가 없으면 사용하지 않는다.

- “사이트가 열리므로 서비스가 정상이다.”
- “Fork가 있으므로 배포가 자동이다.”
- “메일 API 호출이 성공했으므로 사용자가 메일을 받았다.”
- “버튼이 있으므로 공식 신청이 완료됐다.”
- “컴파일되므로 기능이 완성됐다.”
- “CI가 초록이므로 UI도 정상이다.”
- “API key가 있으면 기관 연동 승인이 끝났다.”
- “Codex가 배치에 들어갔으므로 모든 프로젝트를 수정했다.”

대신 무엇을 확인했는지, 무엇을 확인하지 못했는지, 다음 조치가 무엇인지 쓴다.

---

## 20. 운영 루프의 한 문장 요약

10분마다 GitHub Actions가 운영 증거를 모으고, 매일 03:00·10:00·16:00·23:00에
Codex가 전체 포트폴리오를 PM·개발·UX·SRE 관점으로 다시 판단하여, 근거 있는 기능·UI·장애
예방 변경을 canonical boclair98에 검증·merge하고, 실제 Fork를 동기화한 뒤 Coders.kr에
배포하고, production 사용자 흐름까지 확인하여 다음 backlog와 함께 보고한다.

이 문서의 목적은 자동화 자체가 아니라, 자동화가 실제 제품 품질 향상으로 이어지게 하는 것이다.
